변경된 코드에 대해 코드 리뷰를 수행한다.

$ARGUMENTS 가 지정되면 해당 파일/디렉토리를 리뷰한다.
지정되지 않으면 `git diff --name-only`로 변경된 파일을 자동 탐지한다.

다음 관점에서 리뷰해줘:
1. 보안 취약점 (인젝션, 시크릿 노출)
2. 성능 이슈 (N+1, 메모리 누수)
3. 가독성 (네이밍, 함수 길이)
4. 유지보수성 (중복 코드, 테스트 누락)

결과를 Critical / Warning / Good 으로 분류해서 보고해줘.
